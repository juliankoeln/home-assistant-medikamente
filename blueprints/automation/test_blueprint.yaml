blueprint:
  name: 💊 Medikament Erinnerung mit Handy
  domain: automation
  description: Erinnerung zur Medikamenteneinnahme auf das ausgewählte Gerät

  input:
    med_name:
      name: Medikamentenname
      selector:
        text:

    erinnerungszeit:
      name: Einnahmezeit
      selector:
        time:

    notify_device:
      name: Gerät für Benachrichtigung
      selector:
        device:
          integration: mobile_app

trigger:
  - platform: time
    at: !input erinnerungszeit

action:
  - service: notify.mobile_app_{{ device_attr(!input notify_device, 'name') | lower }}
    data:
      title: "💊 Erinnerung"
      message: "Bitte {{ !input med_name }} einnehmen"
